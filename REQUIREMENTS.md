# Juggle 接口编排平台 — 系统需求文档（最近需求包）

> 版本：v1.8（2026-09）
> 状态说明：✅ 已实现并提交 / 🔄 进行中 / ⏳ 待规划

---

## 一、需求概述

本批次需求围绕 **AI 能力深化**（大模型节点参数化与工具调用）、**知识库与缓存基础设施**（知识库子系统、Redis 多实例）、**生态开放**（Skills、市场发现/收藏/分享到 GitHub 官方市场）展开，共 4 大模块。

---

## 二、需求明细

### 2.1 流程管理 > 流程定义 > 设计（大模型节点增强）

| 编号 | 需求 | 说明 | 状态 |
|------|------|------|:---:|
| RQ-AI-01 | 大模型节点基础配置 | 可设置供应商与模型、输入变量可选流程入出参及中间变量、图片输入（多模态视觉识别，多选 data URL 变量）、输出目标可选变量/出参/入参 | ✅ |
| RQ-AI-02 | 模型参数 | 可设置是否启用深度思考（enable_thinking，推理模型生效）、温度（0-2）、最大输出字数（max_tokens，0=不限）、随机数种子（seed，0=随机） | ✅ |
| RQ-AI-03 | 工具调用 | 可设置支持的接口、流程等工具（函数调用 tools）：模型按需调用工具，执行结果回传后生成最终回答（最多 4 轮）；接口按参数位置拼 query/body 直接 HTTP 调用（Mock 优先），流程按入参递归执行子流程并返回出参 JSON | ✅ |
| RQ-AI-04 | 技能附加 | 大模型节点可勾选多个 Skills，执行时技能内容拼入系统提示词 | ✅ |

### 2.2 模型助手 > 模型助手管理

| 编号 | 需求 | 说明 | 状态 |
|------|------|------|:---:|
| RQ-CAP-01 | 模型能力绑定 | 供应商可设置支持的 Skill（多选）、接口（多选）、流程（多选）、工具（列表，JSON 存储）；流程 AI 节点按所选供应商过滤技能列表 | ✅ |

### 2.3 市场（发现 / 收藏 / 分享生态）

| 编号 | 需求 | 说明 | 状态 |
|------|------|------|:---:|
| RQ-MK-01 | 市场发现 | 拉取 GitHub 仓库 `pythonHuang/APIWorkflowCanvasDesignNet` 下 market 目录的 index.json（各类型文件信息）到本地 market 目录；可导入本地 market 目录各类型目录下以 {id}.json 命名的文件，id 相同则更新，否则增加 | ✅ |
| RQ-MK-02 | 收藏与过滤 | 可收藏市场条目，支持「只看收藏」过滤 | ✅ |
| RQ-MK-03 | 下载与直接应用 | 条目可下载（导出 JSON 文件）、可直接应用（一键导入本租户，按名称/code 去重） | ✅ |
| RQ-MK-04 | 元信息展示 | 条目显示图标、名称、名称描述、作者、版本号及更新日期等 | ✅ |
| RQ-MK-05 | 分享到应用市场 | 可把自己的流程、接口、模型助手、Skills、报表分享到应用市场（5 类条目） | ✅ |
| RQ-MK-06 | 分享到官方 GitHub 市场 | 分享时填写作者，先在本地 market 生成 {id}.json 文件并更新 market/index.json（同名条目复用原 id）；登录 GitHub 授权（Personal Access Token），自动完成：校验令牌 → 确保 fork → 创建分支 → 写入条目文件 → 基于官方最新 index.json 合并本条索引 → 生成 Pull Request 到 `pythonHuang/APIWorkflowCanvasDesignNet` 并跳转打开；管理员定期审核 PR，合并到项目 market 目录 | ✅ |

### 2.4 基础设施（知识库 / Redis / 流程节点）

| 编号 | 需求 | 说明 | 状态 |
|------|------|------|:---:|
| RQ-KB-01 | 知识库子系统 | 多知识库 CRUD 与配置（切片大小/重叠、文本或向量检索、向量模型）、文档上传自动解析（word/excel/pdf/txt/markdown）切片入库、手动片段、文本关键词检索与向量余弦相似度检索、清洗（去空去重超短）与重新向量化、匹配记录查询 | ✅ |
| RQ-KB-02 | 知识库检索节点 | 流程设计器新增 KB_SEARCH 节点，供 AI 节点 RAG 问答 | ✅ |
| RQ-REDIS-01 | Redis 缓存节点 | 流程设计器新增 Redis 缓存查询/设置节点（key 支持变量模板/JSON 自动解析/过期秒数） | ✅ |
| RQ-REDIS-02 | Redis 多实例配置 | 系统设置新增 Redis 配置页（多实例：地址/端口/密码/库/默认实例 + 测试连接），未配置时节点执行给出明确报错 | ✅ |
| RQ-FLOW-01 | 数据提取节点 | 流程设计器新增 DATA_EXTRACT 节点：输入可选流程入出参与中间变量；提取类型 json（首个对象数组）/code（代码块）/keyword（关键字后到行尾）/between（起止标志）/length（偏移长度）；输出可赋值变量/出参/入参 | ✅ |
| RQ-SKILL-01 | Skill 管理 | 技能 CRUD（名称/分组/描述/内容/启停）、单个导入（JSON 或 markdown，首个 # 标题为技能名）、批量导入、单个/批量导出 JSON | ✅ |

---

## 三、验收要点

1. **AI 节点**：未配置供应商时执行给出明确报错；模型参数正确透传（enable_thinking/max_tokens/seed）；工具调用轮次上限 4 轮，工具异常不中断流程（错误信息回传模型）。
2. **市场发现**：GitHub 仓库不可达/未建立 market 目录时给出友好提示；导入按 market_item_id + item_type 去重更新。
3. **市场分享**：本地文件与 PR 内容一致；index.json 基于官方最新版本合并，不覆盖他人条目；Token 仅存本机浏览器（localStorage），不落库。
4. **多租户**：市场条目平台级共享（TenantId=null），收藏租户级隔离；导入内容落到导入方租户。

---

## 四、关联文档

- [系统架构文档](./Architecture.md)
- [系统详细设计文档](./DesignDoc.md)
- [系统部署文档](./DEPLOY.md)
- [README（含更新日志）](./README.md)
- [数据库表结构](./docs/表结构/表列表.md)
